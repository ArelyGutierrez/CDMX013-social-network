import { Register } from '../src/components/Register.js';

describe('Registration tests', () => {
  test('this component is a function', () => {
    expect(typeof Register).toBe('fuction');
  });
});
