
const getAuth = () => {
  signInWithEmailAndPassword:(auth, email, password) => {
    newUser: (email, password) => {
      return new Promise((resolve, reject) => {
        resolve(onNavigate('/wall')),
          reject('Llena los campos requeridos'),
      })
    }
  }
}

const auth = {
  getAuth: getAuth(),
};

export default 